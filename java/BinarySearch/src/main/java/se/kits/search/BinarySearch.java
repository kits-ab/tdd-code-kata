import java.util.ArrayList;
import java.util.List;

public class BinarySearch {

    /**
     * Binary searches an element
     * 
     * @param element
     * @param sortedList
     * @return -1 if element is not in sortedList, else the index in the list
     */
    public static Integer binarySearch(int element, List<Integer> sortedList) {
        return null;
    }
}
